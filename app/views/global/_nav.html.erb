<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
  <div class="container">
    <div class="navbar-header">
      <button type="button"
        class="navbar-toggle collapsed"
        aria-controls="navbar"
        aria-expanded="false"
        data-target="#navbar"
        data-toggle="collapse"
      >
        <span class="sr-only"><%= t('nav.toggle_nav') %></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>

      <%= link_to root_path, class: 'navbar-brand' do %>
        <span class='glyphicon glyphicon-star' aria-hidden='true'></span> <%= t('global.title') %>
      <% end %>
    </div>

    <div id="navbar" class="collapse navbar-collapse">
      <ul class="nav navbar-nav navbar-right">
        <%- if user_signed_in? %>
          <li class="dropdown">
            <a href="#"
              aria-expanded="false"
              aria-haspopup="true"
              class="dropdown-toggle"
              data-toggle="dropdown"
              role="button"
            ><%= t('nav.account.title') %> <span class="caret"></span></a>

            <ul class="dropdown-menu">
              <li><%= link_to t('nav.account.membership'), membership_path %></li>
              <li role="separator" class="divider"></li>
              <li><%= link_to t('authentication.logout.nav'), destroy_user_session_path %></li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to t('authentication.login.nav'), new_user_session_path %></li>
        <% end -%>
      </ul>
    </div>
  </div>
</nav>
