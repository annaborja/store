<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to t('global.title'), root_path, class: 'navbar-brand' %>
    </div>
    <div id="navbar" class="collapse navbar-collapse">
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= t('account.my_account') %> <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><%= link_to t('account.subscription.title'), subscription_path %></li>
              <li role="separator" class="divider"></li>
              <li><%= link_to t('global.authentication.log_out'), destroy_user_session_path %></li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to t('global.authentication.log_in'), new_user_session_path %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
